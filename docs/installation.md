# Installation

Requirements: Linux (Ubuntu/Debian), bash, git, systemd.

```bash
git clone https://github.com/<user>/linux-server-management.git
cd linux-server-management
chmod +x scripts/*.sh
```

Run a script:

```bash
./scripts/disk_check.sh
./scripts/service_check.sh
sudo ./scripts/backup.sh
```
